# Install PyTorch with CUDA support - use this command instead:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
# OR for CUDA 12.1: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# OR for CPU only: pip install torch torchvision

# Core dependencies
opencv-python>=4.8.0
numpy>=1.24.0,<2.0  # Must be <2.0 for PyTorch compatibility
tqdm>=4.65.0
scikit-learn>=1.3.0

# Required dependencies
scipy>=1.6.0
pillow>=8.0.0
threadpoolctl>=3.1.0
tifffile>=2022.8.12
